<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Parent</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center vh-100 bg-light">

<div class="row g-3 w-50">
    <div class="col-md-6 d-flex my-auto">
        <div class="card w-100" style="height: 600px;">
            <img src="https://mattaryswk.wordpress.com/wp-content/uploads/2022/03/bangunan-mattary-3-1.jpg" class="card-img-top" alt="Placeholder Image" style="object-fit: cover; height: 100%; width: auto;">
        </div>
    </div>
    <div class="col-md-6 d-flex my-auto">
        <div class="card w-100" style="height: 600px;">
            <div class="card-body d-flex flex-column justify-content-center">
                <h4 class="text-center mb-4">Edit Parent</h4>
                <form action="/edit-parent/<%= parent._id %>" method="POST">
                    <div class="mb-3">
                        <label for="parentName" class="form-label">Name</label>
                        <input type="text" class="form-control" id="parentName" name="parentName" value="<%= parent.name %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="parentEmail" class="form-label">Email</label>
                        <input type="email" class="form-control" id="parentEmail" name="parentEmail" value="<%= parent.email %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="parentPhone" class="form-label">Phone Number</label>
                        <input type="tel" class="form-control" id="parentPhone" name="parentPhone" value="<%= parent.phone %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="parentAddress" class="form-label">Home Address</label>
                        <input type="text" class="form-control" id="parentAddress" name="parentAddress" value="<%= parent.address %>" required>
                    </div>
                    <div class="d-grid mb-2">
                        <button type="submit" class="btn btn-primary">Save Changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</footer>
</body>
</html>